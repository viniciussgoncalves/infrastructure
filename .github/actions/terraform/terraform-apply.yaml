name: Terraform Apply

on:
  workflow_call:
    secrets:
      TF_PVE_PROVIDER_ENDPOINT:
        required: true
      TF_PVE_PROVIDER_API_TOKEN:
        required: true
      AWS_ACCESS_KEY_ID:
        required: true
      AWS_SECRET_ACCESS_KEY:
        required: true
      AWS_ENDPOINT_URL_S3:
        required: true
    inputs:
      TERRAFORM_VERSION:
        description: "Versão do Terraform a ser usada"
        required: true
        type: string
      WORKING_DIRECTORY:
        description: "Diretório contendo os arquivos Terraform"
        required: true
        type: string

jobs:
  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    timeout-minutes: 5

    env:
      TF_VAR_endpoint: ${{ secrets.TF_PVE_PROVIDER_ENDPOINT }}
      TF_VAR_api_token: ${{ secrets.TF_PVE_PROVIDER_API_TOKEN }}
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_ENDPOINT_URL_S3: ${{ secrets.AWS_ENDPOINT_URL_S3 }}
      TERRAFORM_VERSION: ${{ inputs.TERRAFORM_VERSION }}
      WORKING_DIRECTORY: ${{ inputs.WORKING_DIRECTORY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Terraform Init
        run: terraform init -no-color
        working-directory: ${{ env.WORKING_DIRECTORY }}

      - name: Terraform Plan
        run: terraform plan -no-color -out .planfile
        working-directory: ${{ env.WORKING_DIRECTORY }}

      - name: Terraform Apply
        run: terraform apply -input=false -no-color .planfile
        working-directory: ${{ env.WORKING_DIRECTORY }}
